# Session handoff, 2026-08-05: flagship revision through Wave 2

Supersedes `SESSION_HANDOFF_2026-08-04.md` for status. Read
`docs/FLAGSHIP_NARRATIVE_PLAN.md` for the instructions; this is the state.

**Branch `flagship-narrative`, worktree `.claude/worktrees/flagship`, HEAD
`a180b12`.** Not pushed. Working tree clean.

## 1. What is done

| commit | what |
|---|---|
| `695d33d` | The plan: thesis, structure, 7 verified defects with dispositions |
| `2e6ee01` | Wave 1 handoff |
| `764076f` | **Figure 1**: generator, 27 tests, generated TikZ + provenance JSON |
| `751bbf5` | Freeze refresh, verified `passed: true` |
| `b15df26` | Merge: front and back narrative rewrite (branch `wave2-frontback`) |
| `a180b12` | Merge: main-body restructure (branch `wave2-body`) |

Waves 1 and 2 are complete. **Wave 3 (adversarial QA) never ran**: both
reviewers died on an API spend limit before producing any output.

## 2. Gate state at `a180b12`, all green

```
PAPER_CHECK: 24 files, 103 labels, 158 refs, 23 cite keys, 12 environments,
             24 tabulars (232 rows)
PAPER_CHECK: OK -- 0 dangling refs, 0 unresolved cites, environments balanced,
             tabular columns consistent, anonymous build clean
STALE_CLAIM: OK -- no stale claims or pointers outside documented exemptions
REGISTRATIONS_VERBATIM: OK -- 7,103 words match across 4 documents
gen_denominator_macros --check: OK on all three layers
ABSTRACT_CHARS 1864 / 1920, margin 56, 281 words   (measured, not estimated)
in-image pytest, job 11680077: 325 passed, 0 skipped
freeze_prepace --verify: passed
```

The in-image expectation in `CLAUDE.md` and `AGENTS.md` is **325**, updated in
the same commit that added the tests, as the convention requires.

## 3. Figure 1

`paper/figures/fig1_cancellation.tex`, generated by `scripts/make_figure1.py`
from committed artifacts, with provenance for every value in
`paper/figures/fig1_values.json`. Inputs from `introduction.tex`.

TikZ, not matplotlib: there is no LaTeX on this host or in the pinned image and
the image has no matplotlib either (probe job 11675341). It becomes vector at
build time.

**THE FIGURE HAS NEVER BEEN RENDERED OR VISUALLY INSPECTED.** The first person
with LaTeX must check it for clipping, overlap, and label legibility. The
generated file says so at the top. Panel geometry is two 6.0cm columns plus a
0.6cm gutter, 12.6cm against about 12.65cm of `\textwidth` at article 11pt, so
it is close to the edge and worth checking first.

Verified facts behind it, so they are not recomputed:

- Cell Qwen2.5-7B / GSM8K, GPTQ vs AWQ. GPTQ 0.7428, AWQ 0.7370, gap 0.0058.
- harmful 0.0912, beneficial 0.0854, churn 0.1766, answer churn 0.2866, n=1,000.
- Seed deltas (GPTQ minus AWQ): +0.022, +0.016, -0.023, +0.002, +0.012.
- `required_n_for_tost` at 2 pp = **2,730**, from `scripts/audit_stats.py`, not
  hand-derived. `reversal_discordance(1000, 0.02)` = 0.0647.
- 5 of 8 winner flips, 0 ties across all 40 (model, task, seed) triples.
- **Sign convention, confirmed against code**: `flipeval/core.py`
  `compute_pair_metrics` defines `harmful = base & ~method` and
  `net = method.mean() - base.mean()`. Since slot4's net is negative and GPTQ
  scores higher, **GPTQ is the baseline role and AWQ the method role**. So
  "9.12% break" means correct under GPTQ, wrong under AWQ.
- **This cell's churn-to-net ratio is 30.4x, the MAXIMUM of the eight**, against
  a median of 12.7x. The caption says so, the introduction says so, and
  `test_flagship_cell_is_the_most_extreme_of_the_eight` fails if it stops being
  true. Do not let a later edit quietly drop that.

## 4. The largest open item: the page target was NOT met

**Estimated body: about 40 article-11pt pages, against the plan's ~21 target and
the brief's 12 to 16.** At the plan's article-to-TMLR ratio that is roughly 20
to 21 TMLR pages. **No page count is measured and none can be**, so treat every
number here as an estimate with a wide band.

The fifteen relocations landed their full budget. The prose-compression targets
in plan §6 did not, in `audit.tex` (est. 10.4 vs 3.60 target), `minigrid.tex`
(6.3 vs 2.50), `atlas.tex` (4.1 vs 1.50), `certification.tex` (3.9 vs 1.75) and
`related_work.tex` (2.9 vs 0.85).

The reason is uniform and is not laziness: what remains in those sections is
mandatory qualifications, retain-verbatim passages and protected disclosures.
Plan §8 says a compression that removes one is a defect rather than a saving,
and §8 outranks §9. Reaching 3.6 pages in `audit.tex` means cutting roughly 250
further prose lines from a section carrying five mandatory qualifications, two
retain-verbatim passages and three protected disclosures.

**This is an author decision and was deliberately left to Amogh.** The reserve
operations that would close the gap, in increasing order of scientific cost, are
in plan §5 and Agent C's reserve list: move `tab:h3-eightcell` and
`tab:atlas-strata` to appendices, delete the certification worked example, move
the artifacts section wholesale, then trim the audit narrative itself. The last
of those is where qualifications start to be at risk.

## 5. Defects: status

| id | what | status |
|---|---|---|
| D1 | `certification.tex` cited rev-1 MMLU `0.390`/`0.137` | **FIXED**, now `0.492`/`0.140` |
| D2 | atlas range said 3B, floor is 1.3B | **FIXED** at all three sites |
| D3 | "ratio holds at every scale" disconfirmed (4.25x to 8.88x) | **FIXED** by deletion, no replacement claim |
| D4 | `5.3\times` divides rounded medians; direct 5.22 / 5.19, pooled 5.40 | **ESCALATED, UNCHANGED.** See below |
| D5 | `tab:audit-sensitivity` hand-typed 10/1/0 | **FIXED**, now macros |
| D6 | locus tier counts hand-typed | **OPEN.** Needs `gen_denominator_macros.py`, another session's file |
| D7 | "one part in twelve" ambiguous denominator | **FIXED**, now "of that requirement" |

**D4 is the top decision waiting on Amogh.** The 5.3x headline appears in the
abstract, introduction, atlas, mini-grid and conclusion, and is public in the
Zenodo release and the published blog post. Correcting the paper alone would
desynchronise the released record. Nothing on this branch changed it, and the
figure deliberately says "about five times" instead, which every derivation
supports (5.19 to 5.40). `test_atlas_ratio_is_about_five_not_five_point_three`
pins the unrounded value so the discrepancy stays visible.

## 6. What Wave 3 would have checked, and did not

Both adversarial reviewers failed on a spend limit with zero output. **No
adversarial review of the integrated manuscript has happened.** The prompts are
worth reusing verbatim; the highest-value checks they were given:

1. Every numeric claim recomputed against `results/`, especially any number that
   moved during the restructure.
2. Whether the flagship cell is honestly presented as an example rather than a
   representative estimate. This is the most likely real-reviewer attack.
3. Whether Figure 1 panel C or the abstract overstates what certification
   concludes.
4. TOST at one-sided alpha .05 corresponds to a **90%** two-sided CI, at every
   site.
5. Figure 1 geometry read as TikZ source, since nobody can render it.
6. Em dashes, stale rev-1/rev-2 claims, and the §9 repetition collapse.

## 7. Ordered next steps

1. **Run Wave 3.** Nothing else should land first; the manuscript has been
   heavily restructured and unreviewed.
2. Decide D4 and the §4 page-versus-qualifications trade.
3. Regenerate `paper/READING_COPY.md` (`paper/tools/gen_reading_copy.py`), once,
   at the end. Not yet done.
4. Close D6 if the generator can be extended safely.
5. When a machine with LaTeX exists: compile, record main-body and total page
   counts, overfull and underfull boxes, and visually inspect the abstract page,
   Figure 1, every main table, the section transitions, the first appendix page
   and the conclusion.

## 8. Standing constraints observed this session

Nothing pushed, no PR, no upload to arXiv, TMLR, Zenodo or Hugging Face, no
history rewritten, no source tarball touched, no signed amendment or frozen
registration altered, no audited source re-fetched, no private capture
published. `appendix_registrations.tex` is byte-identical and still verifies at
7,103 words. Option A is intact.

**Concurrency:** `main` moved during this work (Amendment 4 signed). This branch
was rebased onto `af4d07e` before Wave 2. Run `git log` before assuming HEAD is
yours; another agent session has been active in the parent worktree throughout.

## 9. Worktrees

```
.claude/worktrees/flagship      flagship-narrative   a180b12   integration
.claude/worktrees/w2-frontback  wave2-frontback      94aaabc   merged
.claude/worktrees/w2-body       wave2-body           b7b8b1c   merged
```

Both Wave 2 branches are merged into `flagship-narrative` and their worktrees
can be removed with `git worktree remove` whenever convenient. The commits
survive removal; the checkouts are gitignored, the history is not.
