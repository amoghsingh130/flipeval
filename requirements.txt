accelerate>=0.33
datasets>=2.20
numpy>=1.26
pandas>=2.2
pyyaml>=6.0
scipy>=1.12
torch>=2.3
tqdm>=4.66
transformers>=4.44

# Optional quantization backends used by scripts/build_quantized.py.
# Install the one you plan to use on ICE.
# auto-gptq>=0.7
# autoawq>=0.2
