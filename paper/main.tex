% FlipEval main paper
% Title/framing agreed 2026-07-15; do not change without the author's sign-off.
%
% TRACEABILITY CONVENTION (applies to every file under paper/):
%   Every numeric claim carries a "% SOURCE:" comment naming the committed file
%   (and section/column) it was read from. If a number has no SOURCE comment it
%   is not yet checkable and must be treated as a TODO, not as a result.
%
% Build: pdflatex main; bibtex main; pdflatex main; pdflatex main
% Venue targets, in order: COLM 2027, ACL 2027, NeurIPS D&B 2027.
% Current file set is venue-neutral (article class) so that a style file can be
% swapped in without touching section prose.

\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{natbib}
\usepackage{amsthm}

% Numbered empirical results. Added 2026-07-27 so that the churn-ratio finding
% of \S\ref{sec:minigrid} is a citable numbered result rather than a table
% caption. Reserve this environment for measured findings; it is not for
% definitions, and never for anything the frozen decision rules return.
\theoremstyle{definition}
\newtheorem{result}{Result}

% Marker macro for slots that await the mini-grid. Every H3 quantity in this
% paper must be wrapped in \minigridTODO until all registered cells exist.
\newcommand{\minigridTODO}[1]{\textcolor{red}{[TODO-pending-minigrid: #1]}}
\newcommand{\TODO}[1]{\textcolor{red}{[TODO: #1]}}
% Every figure whose value depends on the atlas cell population is superseded
% pending atlas rev-2 (ruling R5/R7, 2026-07-21). The rev-1 per-cell arithmetic
% is independently confirmed (10 cells, 262/262 fields); what moves is the
% denominator. See docs/ATLAS_REV2_CORRECTION_2026-07-21.md.
\newcommand{\revtwoTODO}[1]{\textcolor{red}{[TODO-pending-rev2: #1]}}
\newcommand{\revtwoBanner}[1]{%
  \begin{quote}\small\textcolor{red}{\textbf{TODO-pending-rev2.}
  Every figure in this section that depends on the atlas cell population is
  superseded pending rev-2: #1 The rev-1 per-cell arithmetic is independently
  confirmed; the denominator is what changes.}\end{quote}}

% =====================================================================
% BUILD MODE -- the ONE switch that separates the two planned submissions.
%
%   \anonfalse  arXiv preprint: named author block, real identifiers.
%   \anontrue   TMLR submission: TMLR is double-blind (JMLR editorial
%               policies), so the author block and every identifier that
%               names the author are suppressed. TMLR explicitly permits an
%               arXiv preprint of a submission, so both builds may exist at
%               once -- they must be produced from THIS source, differing
%               only by this flag.
%
% Every de-anonymising item in the paper is routed through the macros below.
% If you add another identifier -- a DOI, a repo, a handle, an issue number,
% an acknowledgement -- add it HERE and use the macro at the site. A raw URL
% written directly into a section silently breaks the anonymous build, and
% nothing in the toolchain will warn you.
% =====================================================================
\newif\ifanon
\anonfalse

\newcommand{\withheld}{\textsc{[withheld for double-blind review]}}
\ifanon
  \newcommand{\versiondoi}{\withheld}
  \newcommand{\conceptdoi}{\withheld}
  \newcommand{\repourl}{\withheld}
  \newcommand{\dataseturl}{\withheld}
  \newcommand{\repopath}{\withheld}
  \newcommand{\datasetpath}{\withheld}
  % The harness issue is public and its comment carries the author's handle,
  % so the number itself is an identifier under blind review.
  \newcommand{\harnessissue}{an open issue}
  % The reproduced registrations name their decision owner. The name is source
  % text and is not dropped -- it is routed here, so the blind build shows a
  % visible marker instead of a silent deletion.
  \newcommand{\authorname}{\withheld}
  % PDF info dictionary, plain text only -- see \hypersetup below. \authorname
  % cannot be reused for it: it expands to \withheld, which is \textsc markup.
  \newcommand{\pdfauthorstring}{Anonymous authors}
\else
  \newcommand{\versiondoi}{%
    \href{https://doi.org/10.5281/zenodo.21708923}{10.5281/zenodo.21708923}}
  \newcommand{\conceptdoi}{%
    \href{https://doi.org/10.5281/zenodo.21708922}{10.5281/zenodo.21708922}}
  \newcommand{\repourl}{\url{https://github.com/amoghsingh130/flipeval}}
  \newcommand{\dataseturl}{%
    \url{https://huggingface.co/datasets/AmoghSingh123/flipeval-artifacts}}
  % \allowbreak after each slash: these are set in a fixed-width table column
  % and \texttt will not hyphenate, so without break points they overrun it.
  \newcommand{\repopath}{\texttt{github.com/\allowbreak amoghsingh130/\allowbreak flipeval}}
  \newcommand{\datasetpath}{\texttt{huggingface.co/\allowbreak datasets/\allowbreak AmoghSingh123/\allowbreak flipeval-artifacts}}
  \newcommand{\harnessissue}{issue \#3831}
  \newcommand{\authorname}{Amogh Singh}
  \newcommand{\pdfauthorstring}{Amogh Singh}
\fi

\title{Certifying Compressed Language Models:\\ An Audit and a Statistical Toolkit}
\ifanon
  \author{Anonymous authors\\ Paper under double-blind review}
\else
  \author{Amogh Singh\\ Georgia Institute of Technology}
\fi
\date{Draft \today}

% =====================================================================
% PDF INFO DICTIONARY. hyperref leaves /Title and /Author EMPTY unless they are
% set here: \title and \author feed \maketitle, not the info dictionary, so
% every build so far shipped a PDF with blank file-level identification.
%
% pdftitle is written out rather than reusing \title, which carries a \\ that
% has no meaning in a metadata string. Keep the two in sync by hand; there is
% only one title and it has not changed since 2026-07-27.
%
% pdfauthor goes through the \ifanon block above like every other identifier.
% The info dictionary is the one de-anonymising surface a reader never sees on
% the page, which is exactly how a blind submission leaks a name unnoticed --
% so it is routed, not hard-coded. Note that /Creator and /Producer are still
% set by the TeX engine and are not identifying.
% =====================================================================
\hypersetup{
  pdftitle={Certifying Compressed Language Models: An Audit and a Statistical Toolkit},
  pdfauthor={\pdfauthorstring},
}

% Audit denominators and per-claim values, GENERATED from the sealed rev-3
% verdict CSV by paper/tools/gen_denominator_macros.py. Loaded in the preamble
% so every section can consume \AuditEligible, \AuditAssessable, the
% \AuditXtab* cross-tab and \AuditVal{Rnn}{field} instead of typing a count.
% NEVER hand-type an audit count in a section: that is exactly how the retired
% "4 of 12" headline outlived the verdicts that produced it, and it is
% withdrawn. Regenerate this file, do not edit it.
\input{audit_denominators}

\begin{document}
\maketitle

\input{abstract}

\input{sections/introduction}
\input{sections/related_work}
\input{sections/preregistration}
\input{sections/audit}
\input{sections/certification}
\input{sections/atlas}
% sections/sequential removed 2026-07-26: a results-free \section with no
% registration document. Reduced to one sentence in the conclusion and one
% clause in related_work; restore only when the dated registration exists under
% docs/ AND the component has run.
\input{sections/minigrid}
\input{sections/harness_sensitivity}
\input{sections/discriminant}
\input{sections/artifacts}
\input{sections/limitations}
\input{sections/conclusion}

\bibliographystyle{plainnat}
% Bibliography moved refs.bib -> references.bib on 2026-07-27, when the
% placeholder entries were replaced by primary-source-verified ones. Every
% entry in references.bib carries the source URL and date it was read from.
\bibliography{references}

\appendix
\input{sections/appendix_audit_table}
% Detail relocated from the main text on 2026-07-26 (structural trim). Nothing
% was deleted in the move; each file names its source section at the top.
\input{sections/appendix_prereg_detail}
% Added 2026-07-27 (phase 4 trim, candidate 4): atlas construction detail.
\input{sections/appendix_atlas_detail}
\input{sections/appendix_harness_detail}
\input{sections/appendix_artifacts_detail}
% Added 2026-07-31: extraction provenance, inter-pass agreement, and the AI-use
% statement. Previously the paper never said the claim extraction was done by
% language-model agent sessions outside the reproduced registration.
\input{sections/appendix_extraction}
% Added 2026-07-31: the four frozen protocol documents, reproduced. GENERATED --
% regenerate rather than hand-edit, and re-run the word-stream check against the
% frozen sources. Identical in the arXiv and TMLR builds by design.
\input{sections/appendix_registrations}

\end{document}
